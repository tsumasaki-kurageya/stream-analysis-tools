# System tests

Cross-application fixtures, browser tests, and end-to-end scenarios belong here. Unit and component tests stay with the deployment unit they verify.

`fixtures/yt-dlp-live-chat` contains anonymized, version-scoped NDJSON samples
for the Collection Worker parser contract. Raw acquisition artifacts must not
be committed.
